// @flow strict

import * as React from 'react';

type Props = {||};

class Numbers extends React.Component<Props> {
    render() {
        return (
            <div>
                Numbers
            </div>
        );
    }
}

export default Numbers;